# 明日计划 (2026-02-28)

## 1. 诊断 NPC.cs 中的 CS0103 错误
- **问题分析**：在生成的 `TR_DedServ` 项目中，`NPC.cs` 出现了多处 `error CS0103: 当前上下文中不存在名称“num”/“num4”/“num11”`。这通常意味着局部变量的声明被移除，但其引用仍然存在。
- **任务**：
    - 定位 `NPC.cs` 报错的具体行（如 101412, 101581, 102283）。
    - 检查 `MemberSlicingRewriter` 是否意外地干预了方法体内部的逻辑。虽然当前设计是“全保留”或“全清空”，但需要确认是否有其他 `Visit` 方法（如 `VisitIdentifierName` 或 `VisitLocalDeclarationStatement`）在起作用。
    - 验证 `NPC.AI()` 等超大型方法的处理情况，确认是否存在语法树解析或重写时的截断问题。

## 2. 优化局部变量与作用域处理
- **任务**：
    - 确保 `MemberSlicingRewriter` 在保留方法体时，不会对其中的局部变量声明进行任何修改。
    - 检查 `UsageWalker` 是否在分析依赖时误判了局部变量的作用域，导致某些必要的符号被标记为不必要。

## 3. 增强生成器对大规模文件的稳定性
- **任务**：
    - 针对 `NPC.cs`（超过 10 万行）这类巨型文件，测试生成器的性能和准确性。
    - 优化 `ShadowClassGenerator` 的写入逻辑，确保大文件在处理过程中不损坏。

## 4. 编译验证与回归测试
- **任务**：
    - 修复上述问题后，重新运行生成器。
    - 在生成的 `TR_DedServ` 项目上运行 `dotnet build` 或编译器检查。
    - 确保所有 CS0103、CS0115 等错误全部消除，达到 100% 编译通过。
fisher.heightLevel,fisher出现了错误的fisher添加
这个问题估计是ContainsPrunedReference造成的
我已经禁用ContainsPrunedReference,改用依赖图,但是使用依赖图会产生某些符号找不到的情况,很可能是删除了某些外部库或者删除了using语句所导致的,那么需要在依赖图分析上做足够的检查,确保所有符号都能被正确识别,并不要引入外部错误.需要在依赖图上进行相当大的更新,重依赖图上下手,运行一下当前程序看需不需要再次优化速度,并且查看程序处理逻辑优化不必要的结构,并添加合理的单元测试目前单元测试还是太少了
