# Progress

## Current Facts

- 当前实际编码主线是 `src/MinimalRoslynCpg/`，不是 `NewJoern/`，也不是旧 `src/CPG/`。
- `init.ps1` 已改为按优先级探测可执行项目，当前会优先命中 `src/MinimalRoslynCpg/MinimalRoslynCpg.csproj`，仓库级健康检查已重新跑通。
- 当前最小可运行项目：`src/MinimalRoslynCpg/MinimalRoslynCpg.csproj`
- 当前已新增一个独立原型项目：`src/RoslynPrototype/RoslynPrototype.csproj`
- 这个原型项目用于验证 `设计docs/2026-05-02-删除规则-Roslyn-first-一般重设计.md` 的最小设计是否能跑通。
- 当前已实跑通过：
  - `pwsh -File .\init.ps1`
  - `dotnet build .\src\MinimalRoslynCpg\MinimalRoslynCpg.csproj`
  - `dotnet run --project .\src\MinimalRoslynCpg\MinimalRoslynCpg.csproj`
  - `dotnet run --project .\src\MinimalRoslynCpg\MinimalRoslynCpg.csproj .\src\MinimalRoslynCpg\samples\analysis-sample.cs`

## Roslyn Deletion Prototype

- 已落地 `src/RoslynPrototype/` 最小骨架：
  - `RuleMetadata`
  - `RuleContext`
  - `RuleHitNode`
  - `RuleDecision`
  - `RewriteEdit`
  - `DeletionApplicationService`
  - `IDeletionRule`
  - `PrototypeDeletionAnalyzer`
  - `RuleDecisionEngine`
  - `PrototypeRewriter`
- 当前已新增一个最小应用层文件 `src/Application/DeletionApplicationService.cs`，由 `src/RoslynPrototype/RoslynPrototype.csproj` 直接编译，负责：
  - 从命令行参数组装输入
  - 提供默认源码
  - 直接编排 `分析 -> 标记 -> 传播 -> 决策 -> 改写`
  - 格式化 `SeedMarks / PropagatedMarks / Decisions / Edits / RewrittenSource` 输出
- 当前 `Program.cs` 已退化成薄入口，只负责调用 `DeletionApplicationService`。
- 当前测试也已统一改走 `DeletionApplicationService.Analyze(...)`。
- 当前原型已不再自带空壳 `PrototypeGraph`，`RuleContext.Graph` 现在直接使用 `src/MinimalRoslynCpg` 的 `RoslynCpgGraph` 作为图分析核心。
- 当前 `DeletionApplicationService` 在保留 Roslyn `SyntaxNode + SemanticModel` 规则执行链路的同时，也会先调用 `MinimalRoslynCpg.Builder.RoslynCpgBuilder` 为同一份源码构建 CPG。
- 已落地两条最小验证规则：
  - `DeleteSObjectExpressionRule`
  - `DeleteUnreachableMethodRule`
- 当前已按 `2026-05-04-RoslynDeletionPrototype-标记与传播设计冻结版.md` 落地最小两阶段主线：
  - 新增 `MarkRecord`
  - 新增 `PropagatedMarkRecord`
  - 新增 `IRuleMarker` / `IRulePropagator`
  - 新增 `MarkingEngine` / `PropagationEngine`
  - 应用层当前由 `DeletionApplicationService` 直接固定走 `分析 -> 标记 -> 传播 -> 决策 -> 改写`
- 当前 `DeleteSObjectExpressionRule` 直接依赖 Roslyn `SyntaxNode`、`SemanticModel`、`IOperation` 判断表达式是否以目标对象名为根，并已补最小传播：
  - 可从直接命中的表达式提升到逻辑二元表达式候选
  - 可再提升到直接宿主语句或控制结构头部候选
- 当前 `DeleteUnreachableMethodRule` 已迁到 `Mark(...) / Propagate(...)` 两阶段接口，但传播当前仍为空实现。
- 当前 `RuleDecisionEngine` 会把高风险表达式位置最小升级到父 `StatementSyntax`，覆盖 `if/for/while/do/return` 这类结构头部。
- 当前 `RuleDecisionEngine` 已开始落最小规约式决策：
  - 当传播候选提升到 `LogicalAndExpression` / `LogicalOrExpression` 时，可优先把逻辑表达式规约为幸存操作数
  - 当逻辑规约已覆盖当前节点或外层结构时，会产出 `Skip` 避免 rewrite 对同一片结构重复下刀
- 当前 `RuleDecisionEngine` 已开始按两层结构收口决策：
  - `RuleDecisionEngine -> ISyntaxNodeDecisionBehavior[] -> IDecisionBehaviorArbiter`
  - 当前首批行为已落 `LogicalBinaryReductionBehavior` 和 `DefaultDeleteDecisionBehavior`
  - 当前覆盖与跳过规则先内收在 `DecisionBehaviorArbiter`，暂未独立拆 `IDecisionBehaviorCoveragePolicy`
- 当前 `PrototypeRewriter` 已把“表达式删除”与“语句/方法删除”拆开：
  - 表达式当前改为 `default(<type>)` 风格替换
  - 语句和方法当前继续走 `SyntaxEditor.RemoveNode`
  - 逻辑规约场景当前已支持最小 `Replace` 动作
- 当前已实跑通过：
  - `dotnet build .\src\RoslynPrototype\RoslynPrototype.csproj`
  - `dotnet run --project .\src\RoslynPrototype\RoslynPrototype.csproj .\src\RoslynPrototype\samples\delete-s-object-sample.cs --target-name s`
  - `dotnet test .\tests\RoslynDeletionPrototype.Tests\RoslynDeletionPrototype.Tests.csproj`
- 当前 `s` 样例的验证结果：
  - 能产出 `SeedMarks`
  - 能产出 `PropagatedMarks`
  - 能把 `s.Seed + offset` 提升到 `LocalDeclarationStatement` 删除
  - 能把 `if (s.IsReady)` 提升到 `IfStatement` 删除
- 当前不可达函数样例的验证结果：
  - 端到端测试样例仍能命中不可达 `MethodDeclarationSyntax`
  - 当前 CLI 样例文件 `src/RoslynPrototype/samples/unreachable-method-sample.cs` 使用 `MainEntry`，不满足 `Compilation.GetEntryPoint(...)`，所以命中数为 0
- 当前测试覆盖：
  - 一个 `s` 对象端到端流程测试
  - 一个不可达函数端到端流程测试
  - 一个 `LogicalAndExpression` 规约到幸存操作数的端到端流程测试
  - 一组从 `TerrariaTools` MRU / upward propagation / 条件重写测试提炼出的复杂决策样例：
    - 链式 `&&` 只移除目标操作数，保留其余幸存条件
    - 链式 `||` 只移除目标操作数，保留其余幸存条件
    - 父级控制流宿主删除时折叠内部子决策，避免同一子树重复改写
  - 一个违规规则命中未声明 `SyntaxKind` 时抛异常的约束测试
  - 当前测试通过 `DeletionApplicationService` 校验 `seed marks`、`propagated marks`、`decisions`、`edits`、`RewrittenSource` 和规则节点 kind 白名单约束
  - 当前新增 `TestCodeSet` 反射覆盖守门测试，会枚举 `tests/RoslynDeletionPrototype.Tests/TestCodeSet` 下所有 `public const string` 源码样例，并逐个走 `RoslynCpgBuilder.BuildFromSource(...)` 与 `DeletionApplicationService.Analyze(...)`
- 当前决策修复：
  - `DeleteSObjectExpressionRule` 的逻辑二元规约已从“保留另一侧”改为同类逻辑链扁平化后只移除命中的目标操作数，避免 `(ready && s.IsReady) && enabled` 被误写成 `enabled`
  - `DeletionApplicationService.FilterNestedDeleteDecisions` 现在会过滤被 `Replace` 决策覆盖的 `Delete` 决策，避免 `SyntaxEditor` 对同一父子子树重复编辑
- 本次已把删除原型里的 `RuleMatch` 命名统一改为 `RuleHitNode`，代码、测试和仓库级状态文件已同步。
- 本次已把 `src/Rules/Implementations` 下两条规则的 `Mark` / `Propagate` / `Propose` 接口继续对齐到 `src/MinimalRoslynCpg/Analysis`：
  - 新增 `RuleAnalysisHelpers`，规则侧统一通过 `BinaryExpressionAnalyzer`、`CallAndAccessStructureAnalyzer`、`LoopStructureAnalyzer`、`DefinitionStructureAnalyzer` 读取结构信息
  - `DeleteSObjectExpressionRule.Mark` 不再直接裸扫全部表达式后只靠 kind 过滤，而是先经过 Analysis helper 枚举可分析候选
  - `DeleteSObjectExpressionRule.Propagate` 的逻辑宿主和结构宿主提升改由 Analysis helper 统一判断
  - `DeleteSObjectExpressionRule.Propose` 和 `DeleteUnreachableMethodRule.Propose` 复用统一 delete decision 构造
  - `DeleteUnreachableMethodRule.Mark` 的方法候选枚举改走 `DefinitionStructureAnalyzer`
- 当前明确还没做：
  - 基于真实调用图的不可达性求解
  - 基于真实 CPG fact 的 rule matching
  - 参数删除 / 参数列表收缩
  - member-access 局部保留优先于整表达式替换的更细 rewrite
  - 跨文件回写与多文档编辑
- 当前下一步建议：
  - 先把 `RuleDecisionEngine` 从“直接消费全部有效 marks 并统一删宿主”推进到更贴近冻结设计里的“规约后再决定是否继续升级”
  - 再把 `DeleteSObjectExpressionRule` 的 rewrite 从“删整句优先”推进到更贴近设计文档的局部保留
  - 再处理 `unreachable-method-sample.cs` 的真实入口点样例，使 CLI 验证口径和规则前提一致

## Completed So Far

- 已落地最小 Roslyn-native CPG 主干：
  - syntax 节点和 token 节点
  - symbol 节点
  - operation 节点
  - `SyntaxChild`、`TokenChild`、`SyntaxHasOperation`、`OpHasSyntax`
- 已落地第一批 CPG 抽象层：
  - `TypeDecl`
  - `TypeRef`
  - `Reference`
  - `CallSite`
- 已落地类型与绑定边：
  - `DeclaresSymbol`
  - `ReferencesSymbol`
  - `Ref`
  - `HasType`
  - `EvalType`
  - `ReturnsType`
  - `BaseType`
  - `InheritsFrom`
  - `RefersToType`
- 已落地最小调用抽象：
  - `CallSite --CallTargets--> SymbolMethod`
  - invocation operation 到目标 method symbol 的解析
- 已落地 method-local operation-based CFG：
  - 顺序边 `CfgNext`
  - 条件边 `CfgTrue` / `CfgFalse`
  - `if`
  - `while`
  - `for` 的最小回边
- 已落地 intraprocedural reaching-def 风格 `DataFlow`
  - local / parameter use-def 跟踪
  - 基于 method root block 的顺序定义覆盖

## This Session

- 已对照读取 `joern-master` 参考实现：
  - `passes/callgraph/StaticCallLinker.scala`
  - `passes/callgraph/DynamicCallLinker.scala`
  - `passes/typerelations/FieldAccessLinkerPass.scala`
- 已继续对照读取 `passes/controlflow/cfgcreation/CfgCreator.scala`
- 已继续对照读取 `dataflowengineoss/passes/reachingdef/ReachingDefProblem.scala`
- 已继续对照读取 `dataflowengineoss/passes/reachingdef/DdgGenerator.scala`
- 已继续按 `joern-master` 真实主线对齐 `METHOD -> PARAMS -> BODY -> METHOD_RETURN` 的 CFG / DDG 组织方式
- 已继续对照读取 `passes/callgraph/StaticCallLinker.scala` 和 `passes/callgraph/DynamicCallLinker.scala` 的方法身份与动态候选逻辑
- 已继续补 `MinimalRoslynCpg` 的高权重缺口：
  - 给 `RoslynCpgNode` 增加 `Signature`
  - 给方法符号、调用点、类型声明补稳定签名信息
  - 方法 `FullName` 统一为 `ContainingType.Method:ReturnType(ParamTypes)` 形状
  - 新增 `MemberAccess` 节点种类
  - 新增 `AccessesMember` 边种类
  - 为 `IFieldReferenceOperation` / `IPropertyReferenceOperation` 生成独立 member access 抽象节点
  - 为 member access 抽象节点补 `Ref -> SymbolField / SymbolProperty`
  - 对实例成员访问优先按声明类型/继承链推导 `Type.Member` 形式的 access fullname
- 已继续补最小动态调用候选解析：
  - `CallSite -> CallTargets` 不再只连单个静态目标
  - 会按 receiver type、declared types、继承链、接口链扩出同名同签名候选 method symbol
  - 设计思路对齐 `DynamicCallLinker.scala` 的 `name + signature + hierarchy` 主线
- 已继续补方法身份归一和调用候选过滤：
  - method full name 现在会区分 constructor 和 static constructor，分别归一为 `.ctor` / `.cctor`
  - method signature 现在会带 method generic type parameter / type argument 后缀
  - explicit interface implementation 的 method name 会归一到 `InterfaceType.Method` 形状
  - 动态候选不再只按同名同签名盲收
  - 新增 override / base definition / interface implementation 过滤
  - 候选必须同时满足 receiver type、继承链和实际 dispatch 关系
- 已继续补调用图收尾项：
  - `CallSite` 节点现在带 `DispatchKind`
  - method symbol 节点现在也会带 method-level `DispatchKind`
  - dispatch kind 已从粗粒度三分类继续细化
  - 当前可区分 `static` / `interface-dispatch` / `virtual-dispatch` / `override-dispatch` / `interface-implementation` / `extension-instance` / `extension-static`
  - extension method identity 现在统一走 `ReducedFrom` canonical symbol
  - method full name / signature / name 的归一都会先 canonicalize extension method
  - 调用候选在存在源码内候选时会优先保留 internal methods，压低 external fallback
  - 没有更好候选时，仍会回退到 target method 自身，作为当前最小 external fallback
  - callsite signature 现在保留 invocation 视角，不再被 canonical extension symbol 完全覆盖
  - fallback 现在明确按 `internal -> external -> target method` 优先级选择
  - 新增独立的 super-class fallback 候选阶段
  - method generic suffix 已升级为更稳的 instantiation key
  - generic type parameter 不再只按名字建 key，而是带 `ordinal:name` 形状
  - method symbol dispatch 和 callsite dispatch 已拆层，不再混用同一套语义
  - 新增 method fullName 索引和 `name+signature` 索引，支撑更稳的 exact/external fallback
  - callsite fullName 现在保留 invocation 视角，method symbol fullName 保留 canonical definition 视角
  - 继承链遍历现在带缓存和去重，减少重复候选扫描
  - extension method 候选现在会看 receiver 参数类型，不再只按名字和签名盲收
  - call target fallback 现在带显式排序，不再只是分 internal/external 后直接返回
  - 排序当前综合考虑：internal 优先、exact fullName、exact lookup key、base-definition/override、receiver proximity、extension 加权
  - dispatch 语义现在显式带 `internal-` / `external-` 前缀，不再把源内外方法混在同一类
  - 已补外部调用样例，验证 external dispatch / fallback 的实际路径
- 已继续补 CFG 的 `break/continue` 支持：
  - 新增 `OpBreak` / `OpContinue`
  - 顶层顺序边遇到 `return/break/continue` 不再机械连到下一句
  - `while` / `for` 条件现在会补最小 `CfgFalse` 出口边
  - 循环体里的 `continue` 会跳回条件节点
  - 循环体里的 `break` 会跳到循环后的下一条 sibling operation
- 已继续补 CFG 的 `switch` 和 `try/catch/finally` 最小语义：
  - 新增 `OpSwitch` / `OpTry` / `OpCatch`
  - block 内顺序边改成递归处理，不再只覆盖 method root block 的第一层 statement
  - `switch` 条件现在会连到各个 case body 的首个可执行 operation
  - 没有 `default` 的 `switch` 会补最小 `CfgFalse` 到 switch 后继
  - `switch` 里的 `break` 会跳到 switch 后继
  - `try` 节点会连到 try body 入口
  - try body 会连到 catch body 入口
  - try/catch 会连到 finally body 入口
  - finally body 会连到 try 语句后的下一条 sibling operation
- 已继续补最小 DDG 风格 `DataFlow`：
  - `DefinedSymbol` 现在覆盖 local / parameter 之外的 field / property 赋值
  - variable initializer 的 value 会连到 declarator operation
  - assignment 的 RHS value 会连到 assignment operation
  - invocation 的 receiver 和 arguments value 会连到 invocation operation
  - return value 会连到 return operation
  - `UsedSymbols` 现在覆盖 field / property reference，不再只看 local / parameter
- 已继续补方法抽象、synthetic CFG 和最小 call semantics：
  - 新增 `Method` / `MethodParameter` / `MethodEntry` / `MethodExit` 抽象节点
  - 方法声明现在会生成独立 method abstraction，并挂 parameter abstractions
  - 新增 `ParameterLink` 边，连接 `Method -> MethodParameter`
  - CFG 现在为源码内方法补 synthetic `entry -> first-op` 和 `return/terminal -> exit`
  - `DataFlow` 现在会用 `MethodParameter` 作为参数定义起点
  - invocation arguments 现在会连到源码内目标方法的 `MethodParameter`
  - 源码内方法 `MethodExit` 现在会回流到 `CallSite`
- 已继续补返回抽象和更细的调用/CFG 语义：
  - 新增 `MethodReturn` 抽象节点，把返回值语义从 `MethodExit` 中拆开
  - return value 现在先流向 `MethodReturn`，再流向 `MethodExit`
  - 内部调用现在由 `MethodReturn -> CallSite` 表达返回回流
  - extension method 的 receiver 现在会映射到第一个参数抽象，不再和显式 arguments 错位
  - `try/finally` 内的 return 现在会先连到 finally 入口，再离开方法
- 已继续按 `joern` 的 flow graph 组织方式补方法骨架：
  - `MethodEntry` 现在先串到 `MethodParameter` 链，再进入 method body
  - 无参数方法会直接从 `MethodEntry` 进入 body 或 `MethodReturn`
  - 显式 `return` 现在先 CFG 连到 `MethodReturn`，再由 `MethodReturn` 连到 `MethodExit`
  - 无显式 `return` 的 terminal operation 现在也会汇到 `MethodReturn`
  - 这让当前最小 CFG / DDG 更接近 `CfgCreator.scala` 与 `ReachingDefFlowGraph` 的方法边界组织方式
- 已扩 `samples/analysis-sample.cs`：
  - 增加 `INormalizer`
  - 增加 `BaseNormalizer`
  - 增加 `StepNormalizer`
  - 增加 `override` 调用场景
  - 增加 `break/continue` 场景
  - 增加 `switch` 场景
  - 增加 `try/catch/finally` 场景
  - 增加 property 读写和显式 assignment/value propagation 场景
  - 增加 overload 调用场景
  - 增加 generic method 调用场景
  - 增加 extension method 调用场景
  - 增加 generic receiver extension method 调用场景
  - 增加基类视图 generic override 调用场景
- 已补仓库对齐和 member access 收尾项：
  - `init.ps1` 现在会优先探测 `src/MinimalRoslynCpg/MinimalRoslynCpg.csproj`，不存在时再回退 `NewJoern/NewJoern.csproj`
  - `analysis-sample.cs` 新增 dictionary indexer 读写场景，覆盖 property-family member access
  - indexer 类属性的 `MemberAccess.FullName` 现在会带 `Type.Item:ReturnType[ParamTypes]` 风格签名，不再退化成裸 `Type.Item`
- 已继续按 `joern` 的 field access 组织方式补 member-family 结构边：
  - 对照 `FieldAccessLinkerPass.scala` 的 `base evalType + field identifier/member name` 主线，继续强化成员访问的 base receiver 语义
  - `IFieldReferenceOperation.Instance` 现在显式走 `OpInstance`
  - `IPropertyReferenceOperation.Instance` 现在显式走 `OpInstance`
  - indexer / property 参数现在显式走 `OpArgument`
  - `analysis-sample.cs` 新增 `_peer?.Seed` 条件 property access 场景，补 receiver-sensitive property-family 样例
  - `src/MinimalRoslynCpg/docs/node-edge-catalog.md` 已同步 `AccessesMember` 和 member-family `OpInstance` 语义
- 已继续补 receiver-type-aware member owner 归一：
  - `ResolveDeclaredType` 不再只按成员名找第一个候选类型
  - 当前改为优先命中 exact symbol owner，其次 exact receiver type，再退化到继承链上兼容的 declared type
  - field/property/indexer 的 owner 选择现在会结合 symbol kind 和签名做兼容判断，不再只靠同名匹配
  - `analysis-sample.cs` 新增 `BaseNormalizer.Label` / `StepNormalizer.Label` 场景，覆盖继承链上的同名 property 访问
- 已继续补 conditional/member-chain 视角的 member fullname 稳定性：
  - 对照 `AstForExpressionsCreator.scala` 里把 `?.` spine 重写成普通 member access 的思路，当前 `MemberAccess.FullName` 更强调 access-site receiver 视角
  - 当 receiver type 与 resolved member symbol 兼容时，`MemberAccess.FullName` 现在优先保留 receiver type，而不是过早回落到声明 owner type
  - `Ref` 仍然连向真实 field/property symbol，不改变底层绑定
  - `analysis-sample.cs` 新增 `LabelChain` 条件 property/member chain 场景，覆盖 `?.` + property chain + terminal property 访问
- 已把 `DataFlow` 从顺序传播推进到最小 CFG-sensitive DDG：
  - 对照 `ReachingDefProblem.scala` 的 pred/succ + meet/transfer 主线，当前已基于现有 `CfgNext/CfgTrue/CfgFalse` 建最小 predecessor/worklist 传播
  - 定义传播不再只看 `DescendantsAndSelf()` 顺序，而是按显式 CFG 可达关系汇合 incoming definitions
  - 当前 transfer 会对同 symbol 的旧定义做 kill，再把新定义加入 out set
  - 使用点现在从 CFG-sensitive reaching definitions 里筛同 symbol 定义，再补 `DataFlow` 边
- 已继续补 field/property/call family 的 definition domain：
  - 对照 `ReachingDefProblem.scala` 里 field access 不直接当普通 call kill/gen 处理、以及 `DdgGenerator.scala` 的 container/access-path 思路，当前 definition fact 已从纯 symbol 升级为最小 location key
  - local / parameter 继续走 symbol key
  - field / property 现在带 receiver-sensitive location key
  - call 现在带最小 call-site / target-method location key
  - 这让 kill/gen 不再只按“同 symbol”粗粒度覆盖，而是开始区分 member-family 与 call-family 的定义域
- 已继续补 `FactsMatch` / `FactsConflict` 的 container/alias 规则：
  - 对照 `DdgGenerator.scala` 的 `isContainer` / `isPart` / `isAlias` 主线，当前 field/property/call fact 已带最小 `PathKey`
  - `FactsMatch` 现在不只看 exact location，还会看 container match、part match、同 base 同 path 的 alias-like match
  - `FactsConflict` 对 field/property family 也开始按同 receiver + 同 path 做更细 kill，不再只靠整条 location key 精确相等
- 已继续补 receiver-root 级 kill：
  - 对照 `ReachingDefProblem.scala` 里“局部变量重绑会杀掉其上面的 field access”这条规则，当前 plain local/parameter definition 也会 kill 以其为 base 的 member-family facts
  - `analysis-sample.cs` 新增 `localBox` 重绑场景，覆盖“先取 member，再重绑 base local，再继续取 member”这一类 DDG 语义
- 已继续补 getter-like property summary：
  - 对照 `csharpsrc2cpg` 把 property accessor 建成方法，以及 `DdgGenerator.scala` 对 callsite/return 的 summary 主线，当前 property getter body 也会入图
  - property read 现在会补最小 `receiver -> getter parameter -> getter MethodReturn -> property reference` 风格传播
  - `analysis-sample.cs` 新增 `ComputedSeed` getter 场景，覆盖有真实 getter body 的 property dataflow
- 已继续补 setter-like property write summary：
  - 对照 `csharpsrc2cpg` 的 `set_` accessor 方法形状，当前 property write 也会补最小 `receiver -> setter this/value parameter` 与 `assignment value -> property target` 风格传播
  - `analysis-sample.cs` 新增 `TrackedSeed` setter 场景，覆盖有真实 setter body 的 property write dataflow
- 已继续把 property access 往 call/member 混合语义统一：
  - property read/write 现在都会生成 accessor-like `CallSite` 抽象节点，并补 `CallTargets -> SymbolMethod(get_/set_)`
  - 这让 property family 不再只是 member/reference 语义，也开始显式参与调用图与 call-style summary 语义
- 已继续统一 accessor-like `CallSite` 与 property summary：
  - getter/setter summary 现在不只直连 property operation，也会经过 accessor-like `CallSite`
  - 这让 property 的 member 语义、call 语义、summary 语义开始共用同一条图上载体
- 已继续把 accessor-like `CallSite` 接入现有调用候选体系：
  - property accessor callsite 不再只静态连单个 getter/setter symbol
  - 当前也会走 candidate collection、preferred target ranking、dispatch/fallback 语义
  - dispatch kind 现在会额外区分 `property-get` / `property-set`
- 已继续把 property accessor-like `CallSite` 的候选解析向普通调用收拢：
  - 不再只停在 exact / superclass fallback
  - 当前也会在 receiver hierarchy 上收 override / implementation 候选
  - accessor-like property callsites 现在更完整地复用普通调用的 candidate/ranking/dispatch/fallback 主线
- 已继续把 property/indexer accessor 参数纳入 summary 流：
  - getter/setter summary 现在不只吃 receiver 和 setter value
  - property/indexer arguments 现在也会显式映射到 accessor method parameters
  - `analysis-sample.cs` 已补自定义 indexer 读写场景，验证 indexer accessor 的 parameter flow
- 已继续补 finally-aware CFG terminal propagation：
  - 对照 `CfgCreator.scala` 的 try/catch/finally fringe 思路，当前 `try` / `catch` / `finally` 的末尾可执行 operation 现在会更稳地连到 finally 或后继
  - 这让现有 CFG-sensitive DDG 在异常路径上的定义传播更接近真实执行顺序
- 已继续补 switch-case fallthrough CFG：
  - 对照 `CfgCreator.scala` 的 `cfgForSwitchLike` / case fringe 思路，当前 case body 末尾在未 `break/return/continue` 时会显式流向下一个 case entry
  - 最后一个 case 在未终止时会显式流向 switch 后继
  - 这让当前 CFG-sensitive DDG 在 switch 路径上更接近真实控制流
- 已继续补 empty switch-case forwarding：
  - 当前空 case 会前推到下一个有 body 的 case entry；若后续都为空，则退到 switch 后继
  - 这让 `case 0: case 1: stmt` 一类路径也进入显式 CFG，而不是只在语法上相邻
- 已继续补 try/catch 无 finally 的 terminal propagation：
  - 当前 `try` body 和 `catch` body 在未终止且不存在 finally 时，也会显式流向 try 语句后继
  - 这让 try/catch 路径不再只在 finally 存在时有较完整的终结传播
- 已继续补 empty try -> finally forwarding：
  - 当 try body 为空且存在 finally 时，当前会直接从 `try` 节点流向 finally 入口
  - 这让空 try 场景更贴近 `CfgCreator.scala` 里 empty try 仍执行 finally 的语义
- 已继续细化 property/indexer accessor-like `CallSite` 的解释性：
  - property accessor callsites 现在会区分 `property-*` 与 `indexer-*` dispatch kind
  - setter/indexer-set callsites 的 `TypeFullName` / `EvalType` 不再被 `void` 卡死，而会回落到 property/reference 结果类型
- 已继续细化 resolved dispatch explanation：
  - 普通调用和 accessor-like property/indexer callsites 现在都会标出 `-exact` / `-receiver-exact` / `-hierarchy` / `-fallback` / `-external-fallback`
  - 这让最终命中目标的解释性更强，便于查询和调试
- 已继续补 property-aware candidate ranking：
  - 当调用候选落在 property/indexer accessor 上时，当前排序会额外考虑 associated property 的名称、签名和 receiver 接近度
  - 这让 accessor-like callsites 在 override/indexer 场景下更容易把最合理的 property accessor 顶到前面
- 已新增首个局部 CPG 视图能力：
  - CLI 现在支持 `--view local`
  - 当前可按 `--anchor-id`、`--anchor-full-name` 或 `--anchor-name` 选择唯一锚点
  - 当前会按 hop 数做 breadth-first 子图扩展
  - 当前支持 `--direction both|incoming|outgoing`
  - 当前支持 `--edge-kinds` 限制参与扩展的边种类
  - 当前支持 `--json-out` 导出局部子图 JSON 载荷
- 已把删除原型里的 `DecisionFragment` / `DecisionRelation` / `DecisionUnit` 收拢到 CPG 抽象表示：
  - `DecisionFragment` 现在不再是独立 record 类型，改为 `RoslynCpgNodeKind.DecisionFragment`
  - `DecisionRelation` 现在不再是独立 record 类型，改为 `RoslynCpgEdgeKind.DecisionRelation`
  - `DecisionUnit` 现在以 `RoslynCpgNodeKind.DecisionUnit` 作为单元锚点，并显式持有 fragment 节点、relation 边和最小 syntax binding
  - 删除规则当前通过 `DecisionCpgFactory` 产出决策层 CPG 抽象，而不是再维护一套平行的 fragment/relation record
- 本轮没有回退或修改用户未要求的其他路径。

## Latest Verification

- `dotnet build .\src\MinimalRoslynCpg\MinimalRoslynCpg.csproj`：通过，`0` warning，`0` error
- `dotnet build .\src\RoslynPrototype\RoslynPrototype.csproj`：通过，`0` warning，`0` error
- `pwsh -File .\init.ps1`：通过，已自动定位 `src\MinimalRoslynCpg\MinimalRoslynCpg.csproj`
- `dotnet test .\tests\RoslynDeletionPrototype.Tests\RoslynDeletionPrototype.Tests.csproj --filter DecisionComplexTests --no-restore`：通过，`3/3`
- `dotnet test .\tests\RoslynDeletionPrototype.Tests\RoslynDeletionPrototype.Tests.csproj --no-restore --no-build`：通过，`55/55`
- 默认样例运行结果：
  - `Nodes: 87`
  - `Edges: 161`
- `samples/analysis-sample.cs` 运行结果：
  - `Nodes: 2509`
  - `Edges: 5678`
  - `Method: 19`
  - `MethodParameter: 22`
  - `MethodReturn: 23`
  - `MethodEntry: 19`
  - `MethodExit: 19`
  - `CallSite: 35`
  - `MemberAccess: 49`
  - `SymbolMethod: 27`
  - `SymbolProperty: 10`
  - `SymbolField: 5`
  - `OpBreak: 3`
  - `OpContinue: 1`
  - `OpSwitch: 1`
  - `OpTry: 1`
  - `OpCatch: 1`
  - `OpAssignment: 9`
  - `OpPropertyReference: 9`
- 局部视图 CLI 还未写入这里的新验证结果，待本轮 build/run 后补齐

## Known Blockers

- `progress.md` 与 `feature_list.json` 此前长期停留在旧 `NewJoern/src/CPG` 叙述，本轮已开始纠正。
- 当前方法身份归一已覆盖 constructor/static constructor、generic instantiation key、explicit interface method name 和 extension method canonicalization，并补了 receiver-aware extension 过滤，但还没到完整 specialization 级别。
- 当前动态调用候选已经开始看 override/base-definition/interface implementation，并补了最小 super-class fallback、external fallback、dispatch 分层、方法索引、继承链缓存与去重，但还不是完整 `DynamicCallLinker` 级别。
- 当前动态调用候选已经补了显式排序、internal/external dispatch 语义和部分噪音去重，但更细的 external/dispatch 语义仍可继续精化。
- 当前 member linking 只覆盖 Roslyn 已解析出的 field/property access，尚未扩到更完整的 joern-style field access family。
- 当前 `switch` 和 `try/catch/finally` 已补最小 finally-aware return 路径，但还没有 joern `CfgCreator` 那种更完整的 fringe/jump/case/default 细节和异常传播建模。
- 当前 `DataFlow` 已从最小 local/parameter use-def 扩到 assignment/call/return/property/field 传播，并补了 argument-to-parameter / method-return-to-callsite 的最小源码内 call semantics；方法边界骨架也开始按 `joern` 的 `METHOD/PARAM/METHOD_RETURN` 组织，但还不是完整 DDG，也还没有 CFG-sensitive 求解、kill/gen 精化和更完整 interprocedural 语义。
- 当前局部视图还只是节点锚点 + hop 扩展，不带更高层 query DSL、稳定序列化 schema 或按 span 自动寻锚。
- 当前删除原型的决策层虽然已改用 CPG 抽象表示，但 rewrite 最终仍通过 syntax binding 回落到 Roslyn `SyntaxNode` 执行，还没有把决策层完整并入统一图查询与统一导出面。

## Next Safe Tasks

- 继续补方法身份归一：
  - extension-receiver 特化
- 在局部视图之上继续补：
  - 按 span 自动寻锚
  - 更细的输出格式
  - 面向 method / callsite / member access 的预置 view
- 基于类型层和继承链补动态调用候选，向 `DynamicCallLinker` 靠近。
- 继续把动态调用候选从“最小可用”推进到更接近 `DynamicCallLinker`：
  - 更细的 external fallback
  - 更细的 dispatch 语义
  - 候选排序可配置化/可解释化
- 把 member linking 从 field 扩到 property/indexer 的更稳定路径。
- 继续把 CFG 从“最小结构边”推进到更接近 `CfgCreator`：
  - 更细的 case/default fallthrough
  - 更完整的 try/catch/finally 异常与 return/break 传播
- 继续把 `DataFlow` 从“最小 DDG 风格边”推进到更接近 `ReachingDefProblem` / `DdgGenerator`：
  - kill/gen 规则精化
  - CFG-sensitive use/def 传播
  - 更稳的 method return/outgoing summary 风格边
  - interprocedural call semantics

## Handoff Notes

- 下个会话不要再以 `progress.md` 旧内容假设 `NewJoern` 或 `src/CPG` 是当前主线。
- 继续编码前，优先读：`AGENTS.md`、`progress.md`、`feature_list.json`、`src/MinimalRoslynCpg/Builder/RoslynCpgBuilder.cs`
- 当前最值得继续推进的文件是：`src/MinimalRoslynCpg/Builder/RoslynCpgBuilder.cs`
