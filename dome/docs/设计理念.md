# 设计理念

为什么要多一个port层,因为port层需要整合模型层所提供的能力,本质上实在利用模型层
为什么不把接口和模型放一起,模型可以可以通过多种组合形成新的接口,一个接口的形成可能依赖多种模型所以不放在一起
Analysis模型不保留任何api可能也太激进了或许将纯模型接口(这个接口只依赖于Analysis模型)和Analysis模型放在一起或许
可以降低心智负担.纯模型接口和port放在一起就形成混杂依赖组合和纯模型组合的接口可能会增加心智负担,这里是有待讨论的
